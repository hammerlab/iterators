package hammerlab

package object iterator
  extends bulk
     with count
     with drop
     with end
     with iterator.either
     with group
     with range
     with scan
     with slice
     with sliding
     with sorted
